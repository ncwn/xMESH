
menu "Can5 Logger"

    config CAN5_LOGGER_BUILD_TESTS
        bool "Build LOGGER Tests"
        help
            Select this to build tests for logger component.
            
            IMPORTANT: for this to have effect, CONFIG_LOG_DEFAULT_LEVEL must be  > 0.

endmenu

